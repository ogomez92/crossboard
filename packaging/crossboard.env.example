# Crossboard systemd environment file example
# Copy to: ~/.config/crossboard.env
# Used by packaging/systemd/crossboard.service via EnvironmentFile

# Set arguments for crossboard here. Examples:
#   - Two-way sync with a peer: -k "secret" -m peer.host
#   - Custom sound path: -sound /path/to/copy.wav
#   - Custom listen address: -addr :9876

CROSSBOARD_ARGS=-k "your-secret" -m peer.host -sound ~/copy.wav

